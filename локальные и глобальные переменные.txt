a = 5
b = a + 3

def f(a):
    global b   - позволяет переопределить саму переменную b,
    b = c + a    иначе её значение не изменится.
    return b

c = 1000       - ищется функцией по всей области программы.
print('b before =', b)
print(f(8))
print('b after =', b)

ВЫВОД:
b before = 8
1008
b after = 1008


Таким образом, при отработке функции изменилось значение переменной b благодаря конструкции global b.

Желательно не использовать глобальные переменные в функциях!
